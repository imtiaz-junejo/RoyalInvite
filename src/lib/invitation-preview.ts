/** Fixed design canvas width for builder / editor invitation previews */
export const INVITATION_PREVIEW_DESIGN_WIDTH = 480

/** Fixed design canvas height — 9:16 mobile portrait viewport */
export const INVITATION_PREVIEW_DESIGN_HEIGHT = Math.round(
  (INVITATION_PREVIEW_DESIGN_WIDTH * 16) / 9,
)

/** Hero marketing card design width (purple-gold mockup) */
export const HERO_PREVIEW_DESIGN_WIDTH = 250

/** Safety margin applied after fit-to-viewport scale */
export const INVITATION_PREVIEW_SCALE_SAFETY_MARGIN = 0.9
